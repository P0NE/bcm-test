/**
 * WARNING
 * In real aplication, do not expose this key publicly
 * In a production system we must protect this key using environment variable, secrets vault or configuration service
 */
export const jwtConstants = {
  secret: "secretKeyForTechnicalTest"
};
